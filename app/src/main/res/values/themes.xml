<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.UniCinema" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary_color</item> <!-- Màu cụ thể, ví dụ xanh đậm -->
        <item name="colorPrimaryVariant">@color/primary_variant_color</item>
        <item name="colorOnPrimary">@color/white</item> <!-- Màu chữ/icon trên Toolbar -->
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item> <!-- Status bar trong suốt -->
    </style>

    <!-- Theme cho activity không dùng ActionBar mà dùng Toolbar -->
    <style name="Theme.UniCinema.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Theme cho AppBar -->
    <style name="Theme.UniCinema.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <!-- Theme cho Popup Menu -->
    <style name="Theme.UniCinema.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>